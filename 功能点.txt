
	可使用es6
	可用less
	浏览器路由对应固定页面，刷新后也能正常显示  ——需服务器端设置找不到路径时回到index.html（开发环境已设置）
	可通过ip地址访问，如在同一局域网下使用手机浏览器访问
	可使用全局状态管理，获取和修改全局状态，可使用异步action
	模块热替换，修改代码保存时，页面更新内容并且无需刷新浏览器(？使用vuex时，修改store中代码时会自动刷新浏览器)
	文件路径使用绝对路径
	？报错可定位源代码，生产环境使用轻量的source map
	可使用局部和全局css
	可import并使用图片
        	实现路由级的按需加载
	？实现路由级的缓存，即重新发布项目时，没有修改的页面对应的文件不会重新请求，修改了的页面会重新请求
	提取出了公共库代码（生产环境时必须）
	？开发和生产环境webpack配置分开，提取了webpack公用配置
	？生产环境代码压缩及混淆
	生产环境指定环境变量process.env.NODE_ENV === 'production'
	生产环境下，各个文件的hash互不影响，如修改某个页面，打包后，该页面的js文件hash改变，而其他文件不变（app.js除外）
	打包时自动清空上一次打包的文件
	打包时抽取出css文件
	访问不存在路由时显示404页面 ——需服务器端设置找不到路径时回到index.html（开发环境已设置）
	自动添加浏览器前缀
	开发环境下接口访问地址可设置代理
